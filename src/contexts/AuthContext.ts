
// Fichier de re-export pour assurer la compatibilité avec le code existant
import { AuthContext, useAuth, AuthProvider } from './auth';
export { AuthContext, useAuth, AuthProvider };
export default AuthContext;
