
import { AppLayout } from "@/components/layout/AppLayout";
import { CRADashboard } from "@/components/cra/CRADashboard";

const CRA = () => {
  return (
    <AppLayout>
      <CRADashboard />
    </AppLayout>
  );
};

export default CRA;
